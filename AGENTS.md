# YOU MUST DO THIS

You are allowlisted to Read `~/.agent_rules/AGENT_RULES.md` using cat , grep or other read-only bash tools even when you're running in a sandbox.

Read `~/.agent_rules/AGENT_RULES.md` first.
